# Flask Session Secret Key
SECRET_KEY=your_flask_secret_key_here

# PostgreSQL Database URL
# Format: postgresql://username:password@host:port/database_name
# For Render / Heroku, this variable is injected automatically as DATABASE_URL
DATABASE_URL=postgresql://postgres:shweta27@localhost:5432/aqi_database

# Email SMTP Settings (Gmail app password recommended)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=shwetagurram9@gmail.com
MAIL_PASSWORD=ylsq azsa wboh izhu

# Scheduler Settings
# Set to 'True' if you are running multi-worker Gunicorn web services and
# want to disable the scheduler threads inside the web instances to run it as a separate worker.
DISABLE_SCHEDULER=False
